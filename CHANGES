Requires:
~~~~~~~~~
-
New features:
~~~~~~~~~~~~~
-
Bug fixes:
~~~~~~~~~~
- fixed weak test on python/text learning in classifier.txt
- made learning tests optionnal: if the stemme filter is not
present, they are not ran
- ZODB optim: avoid do no write the _categories persistent mapping if not
  required
- the filters will keep the type presented (str, or unicode) and
not force to unicode, to avoid storage problems

New internal features:
~~~~~~~~~~~~~~~~~~~~~~
- ZODB storage now relies on OOBtrees
